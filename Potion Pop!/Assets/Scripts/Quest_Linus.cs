using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Quest_Linus : MonoBehaviour
{

    private LevelData_Linus level;

    // Start is called before the first frame update
    void Start()
    {
        level = GetComponent<LevelData_Linus>();
    }

    // Update is called once per frame
    void Update()
    {

    }
}
